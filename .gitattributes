# docs/decisions.md is an append-only log, so two concurrent branches conflict
# at EOF by construction; git's built-in 'union' driver keeps both sides and
# drops nothing (no .git/config entry needed -- 'union' is recognized natively).
# Rationale, measurements, rejected alternatives and the accepted failure mode:
# docs/decisions.md, search "lode-4jtc.1".
docs/decisions.md merge=union
